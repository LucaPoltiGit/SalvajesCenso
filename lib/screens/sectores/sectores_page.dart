import 'package:flutter/material.dart';

class SectoresPage extends StatelessWidget {
  const SectoresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Sectores - proximamente'));
  }
}
