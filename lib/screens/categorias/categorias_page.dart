import 'package:flutter/material.dart';

class CategoriasPage extends StatelessWidget {
  const CategoriasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gestionar categorias')),
      body: const Center(child: Text('Gestion de especies, estados y tipos - proximamente')),
    );
  }
}
